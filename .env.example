# =============================================================================
# TalentAI Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values.
# The .env file should NEVER be committed to version control.
# =============================================================================

# Data directory (relative path from project root)
TALENTAI_DATA_DIR=./data

# Rate limiting settings
TALENTAI_MAX_REQUESTS_PER_MINUTE=10

# Model configuration (optional - defaults to config.yaml values)
# TALENTAI_T5_MODEL=t5-small
# TALENTAI_BERT_MODEL=bert-base-uncased

# Feature flags (optional - defaults to config.yaml values)
# TALENTAI_HRI_ENABLED=true
# TALENTAI_DRIFT_ENABLED=true
# TALENTAI_FAIRNESS_ENABLED=true

# Logging level
TALENTAI_LOG_LEVEL=INFO

# =============================================================================
# IMPORTANT SECURITY NOTES:
# =============================================================================
# 1. Never commit secrets or API keys to version control
# 2. Use strong, unique values for any authentication tokens
# 3. In production, use environment variables or a secrets manager
# 4. Rotate credentials regularly
# =============================================================================
